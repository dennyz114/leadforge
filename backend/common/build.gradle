plugins {
    id 'java-library'
}

dependencies {
    // Cosas compartidas entre servicios
    implementation 'org.springframework.boot:spring-boot-starter-json'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    // DTOs, excepciones, utilidades, etc.
}